-- ============================================================
-- DEPRECATED — 表结构已迁移至 Flyway (db/migration)
-- 保留空文件占位，避免旧部署包路径报错
-- ============================================================
SELECT 1;
