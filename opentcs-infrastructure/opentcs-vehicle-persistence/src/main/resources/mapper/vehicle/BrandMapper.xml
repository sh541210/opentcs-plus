<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.opentcs.vehicle.persistence.mapper.BrandMapper">

    <resultMap type="org.opentcs.vehicle.persistence.entity.BrandEntity" id="BrandResult">
        <result property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="englishName" column="english_name"/>
        <result property="code" column="code"/>
        <result property="logo" column="logo"/>
        <result property="description" column="description"/>
        <result property="contact" column="contact"/>
        <result property="enabled" column="enabled"/>
        <result property="sort" column="sort"/>
        <result property="createBy" column="create_by"/>
        <result property="updateBy" column="update_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="delFlag" column="del_flag"/>
    </resultMap>

    <sql id="selectBrandVo">
        SELECT id, name, english_name, code, logo, description, contact, enabled, sort, create_by, update_by, create_time, update_time, del_flag
        FROM tcs_brand
    </sql>

    <select id="selectPageBrand" parameterType="org.opentcs.vehicle.persistence.entity.BrandEntity"
            resultType="org.opentcs.vehicle.persistence.entity.BrandEntity">
        <include refid="selectBrandVo"/>
        <where>
            <if test="brand.name != null and brand.name != ''">
                AND name LIKE CONCAT('%', #{brand.name}, '%')
            </if>
            <if test="brand.code != null and brand.code != ''">
                AND code LIKE CONCAT('%', #{brand.code}, '%')
            </if>
            <if test="brand.enabled != null">
                AND enabled = #{brand.enabled}
            </if>
            AND del_flag = '0'
        </where>
    </select>

</mapper>
