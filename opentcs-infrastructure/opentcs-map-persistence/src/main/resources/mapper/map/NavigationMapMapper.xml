<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.opentcs.kernel.persistence.mapper.NavigationMapMapper">

    <select id="selectPageNavigationMap" resultType="org.opentcs.kernel.api.dto.NavigationMapDTO">
        SELECT m.*, f.name as factoryName, f.factory_id as factoryId
        FROM tcs_navigation_map m
        LEFT JOIN tcs_factory_model f ON m.factory_model_id = f.id
        WHERE m.del_flag = '0'
        <if test="navigationMap.factoryModelId != null">
            AND m.factory_model_id = #{navigationMap.factoryModelId}
        </if>
        <if test="navigationMap.name != null and navigationMap.name != ''">
            AND m.name LIKE CONCAT('%', #{navigationMap.name}, '%')
        </if>
    </select>

    <select id="selectByFactoryModelId" resultType="org.opentcs.kernel.api.dto.NavigationMapDTO">
        SELECT m.*, f.name as factoryName, f.factory_id as factoryId
        FROM tcs_navigation_map m
        LEFT JOIN tcs_factory_model f ON m.factory_model_id = f.id
        WHERE m.factory_model_id = #{factoryModelId}
          AND m.del_flag = '0'
        ORDER BY m.floor_number ASC, m.name ASC
    </select>

    <select id="selectByFactoryModelIdAndFloor" resultType="org.opentcs.kernel.api.dto.NavigationMapDTO">
        SELECT m.*, f.name as factoryName, f.factory_id as factoryId
        FROM tcs_navigation_map m
        LEFT JOIN tcs_factory_model f ON m.factory_model_id = f.id
        WHERE m.factory_model_id = #{factoryModelId}
          AND m.floor_number = #{floorNumber}
          AND m.del_flag = '0'
        LIMIT 1
    </select>

    <select id="selectByMapId" resultType="org.opentcs.kernel.api.dto.NavigationMapDTO">
        SELECT m.*, f.name as factoryName, f.factory_id as factoryId
        FROM tcs_navigation_map m
        LEFT JOIN tcs_factory_model f ON m.factory_model_id = f.id
        WHERE m.map_id = #{mapId}
          AND m.del_flag = '0'
        LIMIT 1
    </select>

</mapper>
